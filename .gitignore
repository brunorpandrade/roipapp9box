# Dependências
node_modules/

# S001 — lockfile não versionado; versões pinadas sem ^ no package.json
package-lock.json

# Next.js
.next/
out/

# Build e cache
*.tsbuildinfo

# Ambiente e segredos (RV: segredos exclusivamente por variável de ambiente)
.env
.env.local
.env.*.local

# Sistema operacional
.DS_Store

# Auxiliares de despacho (não fazem parte do repositório)
artefatos.sha256
verificar_me001.sh
